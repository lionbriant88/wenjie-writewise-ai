# No .env file is read automatically. Supply values through process environment.
DATABASE_URL=
APP_ORIGIN=
AUTH_RATE_LIMIT_SECRET=
DATABASE_CA_CERT=
# CLI management connection only; never use it as DATABASE_URL.
DATABASE_ADMIN_URL=
PILOT_INITIAL_PASSWORD=
# Explicit local development only; never set these on Vercel.
# AUTH_STORAGE=local
# AUTH_LOCAL_PATH=local-private-accounts/local-auth-data
PORT=8793
