# 文阶 WriteWise AI 项目记忆

## 每次任务的强制预检

- 在规划、回答、调试、编辑文件、启动服务或运行命令之前，必须完整阅读本文件和 `docs/current_development_status.md`，先确认与当前任务相关的核心决策。
- 不得凭历史代码、仍保留的旧服务、旧计划文档或上一次会话的印象猜测当前架构。历史记录与当前状态冲突时，以用户最新明确决定和 `docs/current_development_status.md` 的最新记录为准。
- 如果当前实现、运行配置和核心决策不一致，先指出差异并修正方向；如果仍有歧义，停止执行并向用户确认。
- 进行实质性操作前，在进度说明中明确指出本次工作遵循的核心决策。

## 不可违背的主流程决策

- 网站当前唯一主流程是：创建批改任务时，教师可以不上传原题材料，也可以选择上传图片、PDF 或 DOCX；任务必须形成一份当前有效的结构化评分标准，评分标准既可由教师直接填写，也可由 AI 根据材料辅助生成，但页面不得把它们设计或展示为“教师模式 / AI 模式”两条流程；随后按学生上传作文图片、PDF 转换页或设备拍照；作文页按顺序直接发送给多模态大模型；由同一次多模态请求完成学生正文识别、评分和反馈；教师随后查看、修订和确认结果。
- 新作文不得先经过独立 OCR 服务或批改前 OCR 文本确认。OCR Gateway 已退出网站主流程，仅属于历史/保留代码；除非用户明确改变架构决策，否则不得启动、接入或建议 OCR 作为主流程、降级方案或故障绕行路径。
- 教师在批改后修订模型生成的学生原文时，可以通过现有 Grading Gateway 使用确认文本重新批改；这不是恢复独立 OCR 阶段。
- 活动合同使用 `multimodal-grading-request-v2`、`grading-result-v2` 和 `POST /grading/grade-images`。不得恢复旧文本批改入口或 OCR 状态机。

## 当前多模态运行基线

- Provider：Kimi。
- API Base：`https://api.moonshot.cn/v1`。
- 模型：`kimi-k3`。
- Reasoning effort：`low`。
- 最大 completion tokens：`16384`。
- Grading Gateway 超时：`360000` 毫秒（6 分钟）。
- 密钥只能存在于被 Git 忽略的本地环境文件或进程环境中，不得写入本文件、代码、日志、测试 fixture 或提交记录。
- 调试真实批改失败时，必须在直接多模态架构内检查模型配置、鉴权、图片与请求规模、严格 Schema、输出截断、上游状态和结果归一化；不得未经用户改变决策就转向 OCR。

## 当前 AI 成本与吞吐优化决策

- 用户已批准以“最大吞吐目标 + 稳健有界并发”优化全流程；不得把它实现成无上限并发。真实 Kimi 环境必须有显式并发硬上限，调度器在上限内根据成功率、耗时、`429` 和可选 TPM 预算自适应升降。
- 教师一次启动当前任务的全部待批改作文，系统自动排队和补充空闲槽位；单篇失败不得阻断其他作文。Kimi K3 当前不接 Batch API，也不得把多名学生合并进一个模型请求进行识别或逐篇评分；全部逐篇结果形成后允许按下述班级总览决策执行一次去身份化的班级级综合，这不属于多学生联合评分。
- AI 辅助生成评分标准的正常路径只能执行一次 Kimi completion，原材料只发送一次；使用严格 Schema 和本地确定性校验，不再默认执行第二次模型复核或隐藏式模型修复。教师最终创建任务仍承担人工确认。
- 对外继续保持 `multimodal-grading-request-v2`、`grading-result-v2` 和 `POST /grading/grade-images`。Gateway 校验完整任务包后，只向 Provider 投影一份 canonical 任务上下文；必须保留规范化、去重、受限的 `reviewWarnings`，但不得把顶层与 rubric 内重复字段、材料溯源元数据、学生姓名或原题材料发送进逐篇 Prompt。
- 同一任务 revision 的稳定政策和 canonical 任务上下文必须放在 Prompt 前部，并使用服务端生成或确定性派生、绑定 rubric/policy/schema revision 且不含个人信息的 opaque `prompt_cache_key` 帮助 Kimi 自动前缀缓存；不得直接发送教师命名或普通业务 ID。缓存键不是幂等键，缓存是否命中不得影响正确性。
- 第一阶段只做确定性无损输出去重：Provider 不再重复生成最终由 Gateway 根据 transcript 与 `sentencePairs` 重建的 `correctedText` 和 `improvedText`；教师可见的完整 `grading-result-v2` 保持不变。其他语义相近反馈字段只有在真实 A/B 证明质量不退化后才允许继续合并。
- 同一 Gateway 进程连续可用且没有有效内容变更或明确终止后受控重试时，正常 N 篇初次批改应恰好对应 N 次多模态 completion。总 token 不可能与学生人数无关，但必须删除单篇重复上下文和重复输出，并通过并发把全班关键路径由完全串行压缩为受账户能力约束的多轮执行；多实例或重启前必须使用共享或持久幂等 registry 才能继续作出同等保证。
- Gateway 必须保留并安全汇总 Kimi 返回的 `prompt_tokens`、`completion_tokens`、`total_tokens`、可选 `cached_tokens`、`finish_reason` 和阶段耗时；日志不得包含作文正文、图片、Base64、完整 Prompt、学生姓名、API Key 或 Provider 原始响应。
- 同一作文版本与同一评分标准版本必须使用稳定逻辑幂等身份。重复提交、传输重发或结果未知时先复用原 in-flight/成功结果，不得立即产生第二次 Provider 调用；鉴权或配置错误暂停队列，`429` 退避降并发，不可重试错误不得自动重试。
- 图片尺寸或编码优化必须先在合成或已授权匿名样本上做 A/B；正文识别、评分和重要字迹风险达到设计质量门槛后才可启用。不得用未经验证的有损压缩换取 token，也不得把图片优化变成 OCR 阶段。
- 生产或真实 Kimi 运行必须显式选择 Provider；缺失配置时不得静默回退 mock。真实 Key 只允许进入被 Git 忽略的本地环境文件或进程环境，真实基线调用前还必须单独确认样本范围、调用数和费用。
- 上述优化已在 `codex/ai-pipeline-optimization` 分支完成本地实现与 fake/loopback 验收：评分标准正常路径为单次 completion；逐篇 Prompt 使用 canonical 脱敏任务上下文；Gateway 重建两个无损聚合文本；usage/finish reason/阶段耗时安全汇总；同进程 memory registry 复用 in-flight/成功结果；网站以同一稳定请求身份执行全任务有界自适应队列。不得把这些 fake 证据表述成真实 Kimi 的成本、质量或吞吐结论。
- 当前 registry 只在单个 Gateway 进程内有效，不支持重启后或多实例间幂等保证。鉴权、余额、模型权限或配置错误会暂停 Gateway；生产恢复流程是修复问题并重启 Gateway，再由教师在页面显式恢复，触发暂停的作文以原 generation/request ID 重挂。不得新增匿名生产 resume 路由。
- 完整设计与质量门槛见 `docs/superpowers/specs/2026-08-28-ai-pipeline-cost-latency-optimization-design.md`。真实 Kimi 基线、40+40 A/B、100 次 soak、30 篇吞吐和图片变体均尚未运行；25% token、99% 结构化成功率、CER/评分不退化与 60% 吞吐目标均未得到真实证明。Task 16 每一轮仍需单独确认样本、调用数和费用。
- 当前质量 CLI 对缺失或不可认证的人工盲评/soak 审计证据一律 fail closed。本地 JSON 或普通 adapter 不能形成 release pass；完整 Round A 仍缺同一受控 A+B 人工评审协调器。在用户另行决定并补齐该协调器前，不得开始或声称能完成 Task 16。
- 图片仍按原始有序字节发送；没有启用 resize/re-encode，也没有引入 OCR 主流程、降级路径或图片转文本预处理。

## 当前班级总览与 AI 班级总结决策

- 班级总览的当前有效口径是本任务全部“有效成功作文”：结果结构合法、与当前作文源版本和评分标准版本匹配、未删除或作废；教师是否已点击最终确认不是纳入门槛。教师修改过的结果使用修改后的当前版本，单篇最终失败不阻断其余成功作文。成绩通道有效数记为 `N_success`；问题通道完整数记为 `N_issue`，问题通道不完整的 partial 不得被当成“没有问题”。
- 普通共性问题必须同时满足比例和人数门槛：`requiredSupport = max(N_issue < 10 ? 2 : 3, ceil(N_issue × 20%))`，其中同一作文在同一模式中只计一名学生。人数、比例、出现次数和例句均由权威任务服务按证据重算，不得采用模型自报统计；页面必须披露 `N_issue / N_success`。
- 明确低级拼写错误只有在证据确定、修正可由 v2 issue 与唯一 sentence revision/pair 机械交叉验证、无需教师复核且不存在字迹歧义时，才不受频次门槛限制；它们自动进入独立、紧凑的“明确拼写清单”，不能冒充共性问题。`type=spelling` 是正常路径；为覆盖用户明确举例的 `filling → feeling`，certain 的单词级 `word_choice` 只有通过规格中的严格近形词距离与唯一修正规则才可纳入。结果修改、重批或作废后必须确定性重算。
- 班级总结只能在任务队列 settled 且 `N_success >= 2` 后，由教师显式点击生成。正常首次生成执行一次 Kimi completion；查看、统计、拼写聚合、双击、HTTP 重放、刷新重挂和结果检查不得增加 completion。任务级持久 active-generation 唯一约束必须阻止不同标签页用不同 ID 并发计费；教师明确重新生成、来源删除后重新生成或可能已计费的终态失败后明确重试才创建新 generation，并各最多增加一次新调用；`result_unknown` 可信结算前不得新建批次。若合法候选因并发教师编辑进入 `succeeded_unapplied`，应用/放弃均为 0 次调用；覆盖当前教师 AI 文本必须有明确文案、确认和 `aiTextEditRevision` CAS，后续新编辑不得被旧确认覆盖。
- 班级级 Provider 输入来自权威任务服务固定的去身份化快照：全量统计精确计算，非拼写问题使用可审计原子组和有硬字节/组数/token 上限、分层、跨作文轮转的语义投影。`class-review-prompt-budget-v1` 的 Provider Prompt 硬上限为 16,384 tokens，其中可控 payload 最多 15,872，至少预留 512 tokens 给 Provider framing；v1 准入始终取 `max(modelTokenizerCount, utf8ByteCount)`，不允许低估 tokenizer 绕过保守字节上界；framing 预算尚未由当前模型版本基线证明时真实 Kimi 必须 fail closed，超限必须在 Provider 前 0 调用失败。代表片段必须经过内容级身份检测与脱敏，不能安全清除时省略并降低覆盖量。不得发送姓名、班级/任务名称、普通业务 ID、图片、原题材料、作文全文、完整逐篇结果、教师备注或精选素材；投影不足 100% 时必须披露覆盖量，不得宣称全量召回。
- 班级级 Provider 只生成总体评价、主要优点、受控问题组的诊断/教学建议和学习建议；不得重复生成服务端可回填的人数、比例、出现次数和例句，不生成改写练习、独立“主要不足”或独立“教学重点”，也不做 Map-Reduce、Embedding、第二次模型复核或隐藏修复。
- 班级总览采用一个纵向工作页，依次展示当前成绩统计、AI 班级总结、共性问题与建议、明确拼写清单和教师精选素材，不恢复四个一级 Tab。总体评价、主要优点和学习建议通过一个统一编辑入口编辑；共性问题与教师添加问题共用一个可手动排序的列表，“置顶”只表示调整展示顺序，不建立第二套 pinned 状态。明确拼写保持紧凑确定性排序，提升为主要问题后才进入手动排序列表。
- 教师可以在首次 AI 班级总结前从单篇加入问题；权威任务服务为此持久化 task-scoped draft report workspace。workspace 行存在不代表 AI 已生成，页面 CTA 只能依据是否已有成功应用的 AI generation；初次生成失败或候选放弃时，教师问题、教师证据、精选素材和顺序继续保留。report 读取必须原子返回每任务唯一的 `currentGeneration` actionable 摘要，使刷新、新标签页和新设备无需预知 generation ID 即可恢复 active/result-unknown/unapplied 状态。
- 教师后来修改单篇结果时，已有 AI 报告默认保留为生成时快照，不自动重算、不阻断使用、不显示强制“已过期”警告；页面始终显示生成时间和当时纳入数量。教师明确重新生成时原子替换 AI 内容，保留教师添加的问题与排序；若 AI 文本已被教师编辑，必须先确认覆盖；AI 总结仍在编辑或有未保存草稿时不得触发重新生成，必须先保存或取消。
- 教师证据与 system-generation 证据必须逐条区分并绑定精确 result revision；重新生成只替换系统证据，来源页不能用当前结果冒充已经清理或更新的历史 revision。教师项抑制同 topic 的 AI 项时保留当前 system variant；主动移出最后一个教师来源且该 variant 仍合法时恢复为 AI 项并保留位置，来源删除导致整批 AI 失效时则不得恢复。AI 单成员 topic key 必须直接复用 atomic key。任务/来源删除会使相关在途、结果未知和待应用 generation 永久失效，迟到 Provider 结果不得恢复已删除内容。
- 商品化实现必须由权威任务服务持久化任务/结果 revision、generation run、成功报告、AI 文本编辑基线、教师问题、精选素材和唯一排序真源，并在生成、查看、编辑、来源访问和重新生成时校验教师身份、租户与任务归属。Grading Gateway 只承担服务间 synthesis 与全局 Provider 准入，不能充当公网用户安全边界。当前 React 内存状态和单进程 memory registry 只能支持本地原型；持久任务仓库、权限和数据保留规则未完成前不得宣称商业发布就绪。
- 班级总结不修改逐篇 v2 合同。浏览器业务合同使用 `class-review-generation-command-v1` / `class-review-generation-status-v1` / `class-review-report-v1`；权威任务服务到 Grading Gateway 使用 `class-review-synthesis-request-v1` / `class-review-synthesis-result-v1`；Provider Schema 为 `kimi-class-review-output-v1`。完整批准设计与验收门槛见 `docs/superpowers/specs/2026-08-29-ai-class-review-generation-design.md`；本地功能原型已在 `codex/ai-pipeline-optimization` 分支完成 fake 验收，但商业级持久化、认证/租户、跨实例 registry、生产发布集成与真实 Kimi 班级总结质量/成本验收仍未完成，也未获得本轮真实 Kimi 调用授权。

## 当前作文输入决策

- 每位学生对应一张卡片，初始名称按顺序为“学生1”“学生2”……；姓名可编辑，留空时继续使用默认名称。
- 卡片初始只显示学生名称和“+”。点击“+”后只出现“上传相册图片”“上传 PDF 文件”“拍照上传”三个入口。
- 每位学生可上传多张图片；PDF 按页转换为该学生的多页作文。
- 不恢复班级输入、分组方式等已删除的多余上传选项，除非用户明确提出新的产品决策。

## 当前创建任务决策

- 创建任务页保持单页、单一路径，不显示“教师模式”“AI 模式”或要求老师先选择评分标准来源。AI 只作为评分标准区中的可选辅助生成动作；评分标准来源可以作为内部元数据保存，但不得成为用户门槛或界面分支。
- 任务名称为选填项，留空时使用“作文批改任务”；满分为必填且必须合法。
- 原题材料区域标注为“建议上传作文原材料（选填）”。首版支持 JPEG、PNG、WebP、PDF 和 DOCX；PDF 在客户端按页转换为图片，DOCX 只提取正文文本。旧 `.doc` 首版不支持，复杂排版、表格或嵌入图片重要时应建议教师改传 PDF。
- 教师主要填写“写作要求”。页面初始提供一套可编辑默认维度：内容与任务完成 40%、语言质量 40%、结构与连贯 15%、卷面与可读性 5%。维度名称、说明和权重可调整；普通维度可增删；“卷面与可读性”维度不可删除但权重可调；总权重必须为 100%。
- 进入下一步的唯一业务门槛是当前评分标准可用：满分合法、写作要求非空、维度字段合法、恰有一个卷面与可读性维度、权重均合法且总和为 100%。上传材料、调用 AI 生成以及单独点击“确认评分标准”都不是必经步骤。
- 最终按钮“创建任务并上传作文”同时承担教师确认。提交时把当前结构化评分标准写成已确认任务包，然后进入学生作文上传流程。
- 未上传材料时，以教师确认的写作要求构造真实任务上下文，不伪造题目内容。上传材料时只在任务创建阶段理解一次；与教师输入合并时以教师输入为准。材料解析失败不得阻断一份评分标准已合法的任务创建，应明确提示将仅按教师写作要求评分。
- 原题材料不得在每篇学生作文批改时重复发送。学生作文仍直接交给 `kimi-k3` 多模态识别与批改，不得因本创建页改造重新引入 OCR。

## 当前班级总览与共性问题决策

- 本节设计已经用户逐项确认，本地功能原型已完成；正式规格是 `docs/superpowers/specs/2026-08-29-ai-class-review-generation-design.md`；三阶段实施顺序固定为本地功能原型、单独评审后的商业基础设施、集成与发布。商业基础设施与集成发布尚未实施，不得把本地原型或计划文件描述成商品化完成。
- 普通共性问题使用问题通道完整的有效成功作文 `N_issue`，门槛为 `max(N_issue < 10 ? 2 : 3, ceil(N_issue × 20%))`；同一作文在同一问题模式中只计一名学生，出现次数另计。成绩有效但问题通道不完整的结果不进入问题分母。
- 明确、唯一、无字迹歧义的低级拼写错误不受频次限制，进入独立紧凑的明确拼写清单；`filling → feeling` 这类 certain `word_choice` 只有通过版本化的单词级近形、唯一修正和来源重叠规则才可纳入，系统不得用词典、OCR 或语义猜测补造修正。
- 教师可从单篇结果加入未达门槛但有教学价值的问题并随时移出；AI 问题与教师问题共用唯一显示顺序，所谓“置顶”就是排序，不建立第二套 pinned 状态。教师精选素材继续与共性问题分离。
- 班级页收口为一个纵向工作页：成绩统计、AI 班级总结、共性问题与建议、明确拼写清单、教师精选素材。队列 settled 且至少两篇有效成功作文后才显示唯一生成主动作；生成由教师显式触发，不自动调用模型。
- 全量成绩、维度、频次、拼写、人数、比例、例句归属和门槛均由本地/权威任务服务确定性计算。班级级 Provider 只接收去身份化、有界、分层并跨作文轮转的投影，只生成总体评价、优点、受控问题诊断/教学建议和学习建议；正常首次生成恰好增加 1 次班级 completion，查看、统计、排序、应用候选和明确拼写更新均为 0 次。
- 教师修改单篇结果后，已应用报告默认保留生成时快照，不自动重新生成或强制标为过期；教师明确重新生成时使用当前有效 revisions，成功后原子替换 AI 内容，教师加入的问题、证据和排序继续保留。生成期间 AI 文本冲突进入待应用候选，应用/放弃均不得增加模型调用。
- 商业发布必须同时具备权威 Platform 任务/报告/generation registry，以及 Grading Gateway 跨实例持久 Provider execution/result registry 和共享有界准入；任务级 actionable 唯一约束覆盖 `queued | running | result_unknown | succeeded_unapplied`。Platform `running` 只表示 worker 已 claim 并进入 dispatch/attach 生命周期，不能用来计数 Provider 槽；唯一容量真源是 Gateway `active | unknown` lease，待应用候选不占槽。删除必须栅栏迟到结果；未知调用可信结算前回执保持 `pending_provider_settlement`，结算后的 final purge 必须清除可关联 execution identity/payload digest，只能按另行批准政策保留去关联 usage 聚合。
- 班级总结不改变逐篇 `kimi-k3` 直接多模态主流程，不发送学生姓名、图片、作文全文、原题材料、普通业务 ID 或完整逐篇结果，也不引入 OCR。真实 Kimi framing/质量 smoke 仍须另行确认样本、调用数和费用后才可执行。

## 决策维护

- 用户新增、修改或撤销核心架构/产品决策时，必须在开始实现前同步更新本文件，并在需要时更新 `docs/current_development_status.md`。
- `docs/superpowers/` 下旧 OCR 计划及仓库中保留的 OCR 代码只用于历史追溯，不得覆盖本文件的当前决策。
- 完成工作前，逐项核对实现、运行配置、测试和交付说明是否与本文件一致；发现偏离不得宣称完成。
