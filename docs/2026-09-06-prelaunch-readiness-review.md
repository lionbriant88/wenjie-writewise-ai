# 2026-09-06 上线前检查与运营准备

## 当前结论

本轮 18 篇均返回了正文和可用分数，证明当前 Kimi 直接多模态核心链路可以运行；其中仅 3 篇为 `success`，15 篇为需要教师复核的 `partial`，因此没有达到“全部无问题”的放行条件。发现手写分类标签混入、裁边未专项提示、字迹策略不一致及重批分数波动，适合继续受控教师测试，当前不建议直接公开收费运营。

本轮共 21 次 completion、18 次 token 估算请求，按返回 usage 和官方单价保守估算 **8.388968 元（约 8.39 元）**，低于批准的 20 元预算；不是预先扣了 20 元，也不是账户账单核销。租服务器只能解决运行位置，不能补齐质量、账户权限、数据保存、付款与恢复能力。

本次审查对象是 `.worktrees/codex-ai-pipeline-optimization`，分支 `codex/ai-pipeline-optimization`，提交 `ee9c50c`。主目录 `main` 为 `ddc05ce`，尚未合入这份实现；本次没有执行合并或发布。

## 已完成的检查

- 新密钥已保存至上述工作树的 `grading-gateway/.env.prelaunch.local`，Git 已确认忽略。没有写进源码、报告或前端配置，也没有覆盖原 `.env`。
- Kimi 官方中国区 `GET /v1/models` 返回 HTTP 200，目录包含 `kimi-k3`；该项只证明鉴权和模型目录可用，真实批改结果单独统计如下。
- 已本地查看三组全部作文图片，核对页序和样本边界，并提取两份 DOCX 原题材料正文用于准备测试。
- 用户已回复“可以”，批准全部 18 个评分目标、最多 24 次 completion、另最多 18 次 token 估算请求及 20 元预算。真实运行 `smoke-20260906-a` 已完成，没有超出范围或触发预算停止，没有未结算的未知结果预留。
- 验收使用生产 Gateway、Kimi transport/provider、网站结果投影和领域适配器；逐个请求原图并验证同身份重放不增加外部调用。完整作文和结果仅在本机验收进程内存中供视觉核对，磁盘只留匿名状态、分数、用量和计数。此次专用核对页不是产品 UI，因此不能表述为重新走过全部网站按钮或小程序端到端验收。

### 自动化回归

全部命令在优化工作树运行，使用 mock/fake 环境；没有运行 OCR 服务。测试期间以进程级 preload 隔离应用真实 `.env*`，没有移动或读取真实密钥文件。

| 检查 | 最终结果 |
| --- | --- |
| 网站 `npm.cmd test -- --reporter=dot --maxWorkers=2` | 84 文件、1329 项通过 |
| Gateway `npm.cmd test -- --reporter=dot --no-file-parallelism` | 50 文件、1291 项通过 |
| 两端 typecheck | 通过 |
| 网站 lint / build | 通过；构建 429 个模块 |
| Gateway shared-scoring-runtime / grading-policy-fixtures | 通过 |
| 源码工作树与 diff 检查 | 检查时干净，未修改产品源码 |

首轮两端同时默认高并行运行，出现两个 UI 超时及两个 Gateway 时序断言失败；降低测试并行度后，没有修改代码，两端全量通过。高负载下的测试时序稳定性仍需改善。前端测试仍有 `act(...)` 警告，构建有大 chunk 提示。

这些证据不代表真实 Kimi 已达到 99% 结构化成功率、成本降低 25% 或全班吞吐目标，也不覆盖完整 Task 16 盲评/A/B 验收。

## 已整理的真实样本范围

| 来源 | 作文数 | 唯一图片数 | 本轮覆盖 |
| --- | ---: | ---: | --- |
| 应用文 | 5 | 6 | 双页接续、题干排除、明确拼写与语法问题、照片裁边 |
| 读后续写 | 5 | 10 | 三页/双页/单页、最后一页收尾、人物情节与段首衔接 |
| 作文图片 | 8 个评分目标 | 7 | 邀请信 3 篇、横向拍摄学习短文 3 篇、整张答题卡中的两篇分别指定区域 |
| 合计 | 18 | 23 | 分别请求；共 24 个作文图片页次 |

已执行两次原材料辅助 rubric 生成、18 次初次批改、1 次确认文本重批及 19 次同身份结果重放。使用 `kimi-k3` / `low` / 16384 completion tokens、原始图片字节、v2 合同及 `/grading/grade-images`；不引入独立 OCR。真实并发硬上限为 1，此轮不能被表述为吞吐压测。另一项预选文本编辑因实际识别正文没有目标错误而自动跳过，未为凑调用次数新增请求。

样本有明显限制：许多是相近范文的不同手写版本；部分长图右侧文字被裁去；“作文图片”缺完整原题。缺题样本采用明确标记的临时语言/内部逻辑评分要求，不伪造原题或据此宣称任务完成度评分准确。答题卡既有人工分数不作为提供给模型的标准答案；未取得独立教师盲评分，不能计算可靠评分误差或总体识别准确率。

## 本轮真实结果的质量核对

本节记录全部 18 个评分目标的原图与本机内存结果核对发现。`partial` 表示正文和分数仍可用、辅助反馈或评分依据需要教师复核，不等于请求失败，也不等于整篇质量通过。

| 发现 | 本轮证据与含义 |
| --- | --- |
| 多页接续可用 | `case-a01` 的两页正文接续，中文题干未混入；`case-b01` 包含第三页仅剩的收尾，并保留第二段段首 |
| 明确错误可提示 | `case-a02` 对明确的用词与情态动词后动词形式错误给出修改；该篇所有问题引用能在正文中定位，全文修订原文与识别正文一致 |
| 方向与区域处理可用 | 三张横向照片均识别出正文；同一整张答题卡分别指定两篇作文，`case-e01` 与 `case-f01` 没有互相混入，印刷答题说明未进入正文 |
| 手写分类标签混入 | `case-a04` 将页顶“应用文”作为正文首行保留。不能从 `printedTextExcluded=true` 推导正文边界完全正确 |
| 裁边缺失未专项警示 | `case-b04` 第二页右侧行末被照片边界截断，转写仍呈现完整流畅句子，`recognitionWarnings` 与 `legibilityIssues` 均为空；应核对是否按上下文补全，不能当作缺失文字已被可靠识别 |
| 保守字迹策略未完全遵守 | `case-b05` 的说明认为存在合理正确读法，却仍保留 `count_as_legibility_error`，卷面维度为 1.1/1.25。页码经原图核对正确。需修复并复测“可合理辨认则不警告、不扣分”的一致性 |
| 评分依据与关联降级 | 多篇因扣分关联、逐字引文或辅助说明不完整进入 `partial`；`case-b01` 有证据定位回填，`case-c02` 有按有利于学生原则进行的维度分数关联修正。这是安全容错起作用，不能表述为模型原始依据完全一致 |
| 重批链路可用，评分稳定性仍需验证 | `case-a02` 只把一个明确用词错误改为正确形式，零图片重批返回 `success`；经逐字比较没有其他正文变化，但分数由 14/15 变为 13/15。这一次对照不能测出总体稳定率，却说明修正一个错误不保证本次模型评分上升，不能把技术重批成功当作评分稳定性通过 |

只读源码与零网络合成复现进一步确认：缺扣分问题关联、证据不可逐字定位等情况均可能触发“部分维度评分依据不完整”，不是简单漏填 `reason/evidence`。optimized 与 legacy 共用维度 Schema，未发现优化版本独有的字段命名错位。字迹合同目前也缺少“歧义已解决”的结构化状态，无法仅凭合法 JSON 独立排除上述语义矛盾；不应通过匹配中文说明关键词直接消除扣分。

这些发现足以阻止“全部无问题”的结论。后续应先用匿名合成用例收紧正文边界、裁边提示、字迹政策与扣分关联，再对针对性真实样本复测。当前未追加未经本轮范围涵盖的 A/B 或吞吐实验，未更改生产策略以掩盖复核标记。

### 最终调用、结果与费用

| 类别 | completion 次数 | 结果 | usage 估算费用 |
| --- | ---: | --- | ---: |
| AI 辅助评分标准 | 2 | 2 次成功，草案仍需教师确认 | 0.277240 元 |
| 图片初次批改 | 18 | 3 success、15 partial、0 failed | 7.785908 元 |
| 确认文本重批 | 1 | success，0 图片 | 0.325820 元 |
| 合计 | 21 | 全部返回，未自动重试 | 8.388968 元 |

- 另有 18 次官方 token 估算请求，未超过单独上限；不是额外 18 次批改。费用表按 completion 返回 usage 计价，最终以 Kimi 账户账单为准。[估算接口](https://platform.kimi.com/docs/api/estimate)
- 19 次成功/partial 结果重放全部返回相同响应，completion 与 tokenizer 增量均为 0。21 个操作各恰好一次 Provider completion，所有 `finish_reason=stop`；没有输出截断、失败自动重试、未知结果或未结算预留。
- 独立匿名审计重算费用与汇总一致。每次实际输入都低于预留边界、输出都未超过 16384；预算控制使用保守估算与预留，不等同于厂商账单的绝对担保。
- 初批每篇模型成本平均 **0.43255 元**、中位 **0.38295 元**，范围 **0.27752—0.97566 元**。初批 Provider 耗时平均 **62.83 秒**、中位 **56.92 秒**，范围 **39.45—105.68 秒**。全流程各操作耗时合计约 **21 分钟**，包含读图、估算、网关和零付费重放，是串行有限验收时间。
- 全部 21 次调用输入 163333 tokens、输出 53481 tokens，总计 216814。明确返回的缓存输入为 12544 tokens；8 条 usage 含缓存值，13 条缺失，缺失按未缓存单价估算，不把缺失当作“没有缓存命中”的实证。
- 初批 15/18（83.3%）提示复核：14 篇涉及评分依据不完整、4 篇辅助内容不完整、3 篇证据定位回填、2 篇表达优化省略、2 篇自报总分与重算不同、1 篇维度分数与问题关联安全修正；原因可重叠。
- 对全部 19 份最终展示结果（18 初批＋1 重批）做本机 DOM 核查：全文修订原文与 transcript 一致，纠正版/优化版非空，所有问题引用和维度证据均可在最终正文中定位。此项证明最终结构可消费，不证明模型内容判断正确。
- 本轮未完成 40＋40 配对 A/B、100 次 soak、30 篇并发吞吐或图片变体实验，不能宣称达到此前制定的 99% 成功率、25% token 节省、评分不退化或 60% 吞吐提升目标。

匿名运行依据保存在优化工作树的 `grading-gateway/local-private-results/prelaunch-20260906/smoke-20260906-a.summary.json` 与 `smoke-20260906-a.quality-counts.json`，均被 Git 忽略；不包含正文、图片、原始 Provider 响应或凭证。收到 `smoke_finished` 且完成核对后已停止临时验收服务，不保留完整结果的磁盘副本。

## 公开收费与对应功能发布前的准备

| 必须补齐 | 当前代码证据 / 影响 |
| --- | --- |
| 账户、会话与任务归属权限 | `app/src/context/AppStateContext.tsx` 及 Gateway 用户路由尚无完整教师身份/租户边界；CORS 不能代替认证 |
| 数据库与私有图片存储 | `AppStateContext.tsx:466` 起使用 React 内存状态；刷新、换设备、后台重启后不保证恢复 |
| 持久后台队列与幂等 | 已有有界调度，但 `essayGradingRegistry.ts:232`、`providerAdmissionController.ts:66` 使用单进程 Map；单实例试运营也须持久保存执行结果、幂等身份和未知状态并验证重启恢复；多实例时再补共享准入，避免重启/扩容重复计费 |
| 订单、支付、额度、退款与对账 | Provider token 遥测已实现，用户付费权益账本尚未实现 |
| 班级总结真实集成（发布该功能前） | `AppStateContext.tsx:501` 默认 fake client，`:93` 内容身份检测仍为原型占位；生产 framing calibration 为 null；当前不能当作已上线真实 AI 功能售卖。网页版可先关闭该原型功能 |
| 微信小程序（发布小程序前） | 只有设计/计划，尚无可提交审核的小程序应用代码；可与网页版分阶段交付 |
| 部署和运维 | 需要生产启动、HTTPS/反向代理、备份恢复、报警、用户与账户费用上限；网站和 Gateway 的 v2 合同需配套发布 |

以上是在现有项目内补齐商业基础设施的工作，不需要改变 Kimi 直接多模态架构。当前检查没有实施这些功能或擅自决定数据库、认证、存储供应商。

## 用户需要提供或决定什么

1. **运营主体与所在地**：拟使用公司或个体工商户的主体资料、联系人、客服电话及收款/开票安排。先明确面向教师的工具范围、试用人数和预期月作文量。
2. **实名云账号与域名**：国内云账号、主体实名认证域名、DNS 授权与月预算。云账号和支付账号由用户持有，后续以最小权限协作。
3. **微信账号**：小程序账号、真实服务类目、AppID、认证和备案信息。常规商户小程序支付需商户号、JSAPI 权限、AppID 绑定；次数包或会员还应核实当前虚拟支付政策及终端费率。[微信支付接入准备](https://pay.wechatpay.cn/doc/v3/merchant/4015459512)
4. **产品收费规则**：按次/次数包或订阅、试用额度、何时消耗额度、结果未知和失败如何处理、有效期、退款和发票规则。先取得真实批改成本再定价，避免无限量套餐带来无界支出。
5. **学生数据处理规则**：样本/实际运营数据的合法使用依据、学生年龄范围、学校/教师与平台责任、保存期限、删除渠道、隐私政策、投诉渠道。涉及不满 14 岁个人信息需特别处理监护人同意与专门规则；委托和敏感信息处理等还涉及影响评估。[个人信息保护法](https://www.cac.gov.cn/2021-08/20/c_1631050028355286.htm)

面向中国大陆运营，还需办理适用的网站 ICP 备案、小程序 APP 备案及公安联网备案。收费是否构成经营性互联网信息服务、教育类服务是否需要前置文件，应依据准确业务说明向属地主管部门核实，不能单凭“收费”或“SaaS”作绝对判断。[工信部 APP 备案通知](https://www.miit.gov.cn/zwgk/zcwj/wjfb/tz/art/2023/art_920db564162e4312916a01bed6540ad8.html)、[互联网信息服务管理办法](https://qhca.miit.gov.cn/zwgk/zcwj/flfg/art/2022/art_21d4b34dc211467db385c3b4a690d8b7.html)、[公安备案官方办事指南](https://gjzwfw.www.gov.cn/fwmh/item/v3/item_113406000030841746300070911400101.do)

直接调用已备案模型的应用，应按产品实际适用范围向地方网信部门核实应用登记等要求，核对 Kimi 服务与备案对应关系，并安排 AI 标识；不能把底座备案等同应用已完成全部手续。[2026-05-13 网信办公告](https://www.cac.gov.cn/2026-05/13/c_1780413225190669.htm)、[生成式 AI 暂行办法](https://www.cac.gov.cn/2023-07/13/c_1690898327029107.htm)、[生成合成内容标识办法](https://www.cac.gov.cn/2025-03/14/c_1743654684782215.htm)

微信个人主体和虚拟支付的当前适用条件，本次未能从官方页面核实，不能给出“个人一定不能收费”或“iOS 一定不能购买”的结论。应在真实微信后台按类目、主体和终端政策确认；不以第三方近期传言作为上线依据。

## 预算参考（查询日期 2026-09-06）

不需要购买 GPU 服务器，模型运行在 Kimi。起步可评估国内 2 核 4GB 或 4 核 8GB 普通云主机，加数据库、私有对象存储与备份；最终规格以实际上传规模和压力测试为准，不是现有容量承诺。

- 腾讯云轻量通用型 2 核 4GB、90GB SSD、6Mbps、800GB/月的当前列表价例为 **80 元/月**，这只是主机费用。[官方价格](https://cloud.tencent.com/document/product/1207/73452/)
- 可先预留 **300—800 元/月**作为早期云资源、存储、备份和监控的规划预算；这是预算假设，另计模型、域名、短信、认证、支付渠道、税务、人工与合规服务。
- Kimi K3 官方中国区每百万 token：未命中缓存输入 **20 元**、命中缓存输入 **2 元**、输出 **100 元**。[K3 官方定价](https://platform.kimi.com/docs/pricing/chat-k3)
- 本轮 18 篇初批实测 usage 估算平均 **0.43 元/篇**，单篇约 **0.28—0.98 元**；这仅代表此次样本规模与模型配置，不是所有作文的固定成本。运营定价还需计入任务材料/rubric、教师重批、有用量的失败、支付渠道、税费和客服，不能据此承诺无限量套餐。

建议先补齐共享后端与收费闭环，以网页版做有限教师试运营，再让小程序复用同一账户、任务、结果和权益系统。最终次序由用户决定；本轮没有购买资源、注册主体、提交备案、接入支付或发布网站。
